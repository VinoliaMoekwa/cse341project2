{
  "swagger": "2.0",
  "info": {
    "title": "Orders and Products API",
    "description": "API for managing orders and products",
    "version": "1.0.0"
  },
  "host": "cse341project2-2.onrender.com",
  "basePath": "/",
  "schemes": ["https"],
  "paths": {
    "/orders": {
      "get": {
        "tags": ["Orders"],
        "description": "Retrieve all orders",
        "responses": {
          "200": { "description": "OK" }
        }
      },
      "post": {
        "tags": ["Orders"],
        "description": "Create a new order",
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "schema": {
              "type": "object",
              "properties": {
                "customer_name": { "type": "string", "example": "any" },
                "quantity": { "type": "integer", "example": "any" },
                "total_price": { "type": "number", "example": "any" },
                "order_date": { "type": "string", "example": "any" },
                "shipping_address": { "type": "string", "example": "any" },
                "province": { "type": "string", "example": "any" }
              }
            }
          }
        ],
        "responses": {
          "201": { "description": "Created" },
          "500": { "description": "Internal Server Error" }
        }
      }
    },
    "/products": {
      "get": {
        "tags": ["Products"],
        "description": "Retrieve all products",
        "responses": {
          "200": { "description": "OK" }
        }
      },
      "post": {
        "tags": ["Products"],
        "description": "Create a new product",
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "schema": {
              "type": "object",
              "properties": {
                "name": { "type": "string", "example": "any" },
                "category": { "type": "string", "example": "any" },
                "price": { "type": "number", "example": "any" },
                "stock": { "type": "integer", "example": "any" }
              }
            }
          }
        ],
        "responses": {
          "201": { "description": "Created" },
          "500": { "description": "Internal Server Error" }
        }
      }
    }
  }
}
